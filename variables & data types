/* variables - переменные - var
semicolon - точка с запятой - строка завершена */
var num; // 1. переменная существует
num = 10; // 2. задать значение переменной

var num = 5; // упрощенный вариант



/* remainder operator % оператор остатка от деления 
5 % 2 = 1 because
Math.floor(5 / 2) = 2 (Quotient)
2 * 2 = 4
5 - 4 = 1 (Remainder) */
const myRemain = 11 % 3; // 2 вывод

