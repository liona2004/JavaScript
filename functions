function multiply(a, b){
  return a * b; // return - оператор, в том месте, где будеет вызвана функция, будет подставлено то, что стоит после return, теперь эту функцию можно использовать в других математических выражениях и расчетах
} // function - ключевое слово объявляет функцию, multiply - имя функции, a и b - параметры функции. В фигурных скобках тело функции, выполняемый код
multiply(); // вызов функции
multiply(5,5); // 25 вывод
